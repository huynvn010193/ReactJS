import React from 'react';
import ArticleForm from 'components/articleForm';

const CreateArticle = () => {
   const onSubmit = (data) => {
      console.log(data);
   }

   const initialValues = {
      title: 'foo',
      description: 'bar',
      body: 'baz',
      tagList: 'vvvv'
   }
   const errors = {}
   return (
      <ArticleForm onSubmit={onSubmit} initialValues={initialValues} errors={errors} />
   )
}

export default CreateArticle;